{
    "version": 2,
    "builds": [
      {
        "src": "server.js",
        "use": "@now/node"
      }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/server.js" },
      { "handle": "filesystem" }
    ],
    "env": {
      "NODE_ENV": "production"
    }
  }
  